<p id="notice"><%= notice %></p>

<div class="container">
 <div class="row">
  <div class="col-md-offset-2 col-md-8">
   <div class="panel panel-dark">
    <div class="panel-heading">
      <h2><strong>drvLetting Go</strong></h2>
      <span>from: <em><%= link_to @drivet.user.username, profile_path(@drivet.user.id) %></em></span>
    </div>
    <div class="panel-body">
      <div class="well well-lg">
        <%= @drivet.message %>
      </div>
      <% if user_signed_in? && current_user = @drivet.user %>
         <%= link_to 'Edit', edit_drivet_path(@drivet), class:"btn btn-default" %>
       <% end %>
    </div>
   </div>
  </div>
 </div>
</div>
